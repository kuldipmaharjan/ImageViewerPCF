export type imageViewerData = {
    name: string;
    original: string;
    thumbnail: string;
}

export type imageRawData = {
    name: string;
    type: string;
    size: number;
    content: string;
}